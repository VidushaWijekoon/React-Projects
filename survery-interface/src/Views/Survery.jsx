import PageComponent from "../Components/PageComponent";

const Survery = () => {
    return (
        <>
            <PageComponent title={Survery}>Survey Content</PageComponent>
        </>
    );
};

export default Survery;
